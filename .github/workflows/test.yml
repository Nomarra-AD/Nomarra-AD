name: Build and Test
on: [push]
jobs:
  - name: Checkout code
    run: echo $GITHUB_EVENT_NAME > code.txt
  - name: Run mock build
    run: echo 'Mock build successful' > build.txt
    success: [always]